box: node:10

build:
  steps:
    - script:
        name: init .clasprc.json .clasp.json
        code: |
          echo $CLASPRC_JSON > .clasprc.json
          echo $CLASP_JSON > .clasp.json
    - script:
        name: clasp install
        code: npm i -g @google/clasp
    - npm-install
    - script:
        name: push
        code: |
          npm run push